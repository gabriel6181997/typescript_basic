"use strict";
var _a, _b, _c;
const quill = {
    name: 'Quill',
    role: 'front-end',
    follower: 1000
};
function toUpperCase(x) {
    if (typeof x === 'string') {
        return x.toUpperCase();
    }
    return x;
}
const upperHello = toUpperCase('hello');
const upperHello2 = toUpperCase;
upperHello2('h1');
upperHello2(32);
const upperHello3 = function (x) { return 0; };
let intersectionFunc;
intersectionFunc = function (a, b) { return 0; };
let unionFunc;
unionFunc = function (a) { return 32; };
function describeProfile(nomadWorker) {
    console.log(nomadWorker.name);
    if ('role' in nomadWorker) { // whether there is any role in nomadWorker
        console.log(nomadWorker.role);
    }
    if ('follower' in nomadWorker) {
        console.log(nomadWorker.follower);
    }
}
class Dog {
    constructor() {
        this.kind = 'dog';
    }
    speak() {
        console.log('bow-wow');
    }
}
class Bird {
    constructor() {
        this.kind = 'bird';
    }
    speak() {
        console.log('tweet-tweet');
    }
    fly() {
        console.log('flutter');
    }
}
function havePet(pet) {
    pet.speak();
    switch (pet.kind) {
        case 'bird': //Way of adding tag by using switch (same as instanceOf)
            pet.fly();
    }
    if (pet instanceof Bird) { //object generated by Class 'Bird'
        pet.fly();
    }
}
havePet(new Bird());
// havePet({speak() {console.log('hello')}, fly() {console.log('not fly')}})
// const input = document.getElementById('input') as HTMLInputElement;
// input.value = 'initial input value';
document.getElementById('input').value = 'initial input value';
const textbook = document.getElementById('textbook'); //!= not null
const designer = {
    name: 'Quill',
    role: 'web',
};
const downloadedData = {
    id: 1
};
console.log((_b = (_a = downloadedData.user) === null || _a === void 0 ? void 0 : _a.name) === null || _b === void 0 ? void 0 : _b.first); // ? = stop undefined error
const userData = (_c = downloadedData.user) !== null && _c !== void 0 ? _c : 'no-user';
let target = 'hello';
let source = 'hello';
target = source;
var Color;
(function (Color) {
    Color[Color["RED"] = 0] = "RED";
    Color[Color["BLUE"] = 1] = "BLUE";
})(Color || (Color = {}));
let target2 = Color.RED;
let source2 = 0;
target2 = source2;
let target3 = function (a, b) { };
let source3 = function (a) { };
target3 = source3;
target3('hi', 'hello'); //the second one is ignored.
class AdvancedPerson {
    constructor() {
        this.name = 'Peter';
        this.age = 35;
    }
}
class AdvancedCar {
    constructor() {
        this.name = 'Prius';
        this.age = 5;
    }
}
let target4 = new AdvancedPerson();
let source4 = new AdvancedCar();
target4 = source4;
function advancedFn(...args) {
}
advancedFn(0, 'hi', true, 3, 3, 3, 3, 3);
const milk = 'milk';
let drink = milk;
const array = [10, 20];
const Peter = {
    name: 'Peter',
    age: 38
};
